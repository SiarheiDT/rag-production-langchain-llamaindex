Retrieval-Augmented Generation (RAG) is a technique that combines document retrieval with language generation.
The system retrieves relevant documents and uses them as context for generating answers.
This approach reduces hallucinations and improves factual accuracy.